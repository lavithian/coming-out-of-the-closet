
<div class="show-container">
  <div class="show-img">
    <% if @costume.photo.attached?  %>
      <%= image_tag @costume.photo, height: 500, width: 350, class:"rounded shadow cos" %>
    <% else %>
      <%= image_tag "No-Image-Placeholder.png", alt: 'No image available' %>
    <% end %>
  </div>
  <div class="show-details">
    <h1><%= @costume.name %></h1>
    <h4>$<%= @costume.price %> per night | Owner: <%= @costume.user.username %></h4>
    <p id="desc"><%= @costume.description %></p>
    <%= render 'bookings/new' %>
    <div class="back">
      <a href="/costumes" id="back-to-index">Back to Costumes!</a>
    </div>
  </div>
</div>
